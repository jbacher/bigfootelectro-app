import 'package:flutter/widgets.dart';

// Maybe make this more reactive but sticking with basics for now
// final Map<String, Widgetbuilder> routes = <String, WidgetBuilder> {
//   // "/": (BuildContext context) => homescreen
// } 